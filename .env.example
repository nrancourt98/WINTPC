# Connection string for your existing Postgres instance.
DATABASE_URL="postgresql://user:password@host:5432/wintpc?schema=public"

# Directory (inside the container) where uploaded part photos are stored.
# Mount a Docker volume at this path so photos survive container rebuilds.
UPLOAD_DIR="/data/uploads"

# Port the app listens on inside the container.
PORT=3000
